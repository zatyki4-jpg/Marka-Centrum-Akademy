{
  "name": "dakpro-backend",
  "version": "2.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:build": "rm -f database.demo.db && sqlite3 database.demo.db < schema.sql && sqlite3 database.demo.db < seed.sql && echo 'Database klaar!'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "sqlite3": "^5.1.7",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
